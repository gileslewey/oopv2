/* Treehouse FSJS Techdegree
 * Project 4 - OOP Game App
 * app.js */

const game = new Game;
const startButton = document.getElementById('btn__reset');
startButton.addEventListener('click', function(){
game.startGame();
});

class App {
constructor(){


// {
// new Game();
// console.log('game started');
// };





   }
 }

 // const logPhrase = (phrase) => {
 // console.log(`Phrase - phrase: `, phrase.phrase);
 // };
 // const game = new Game();
 // console.log(game.getRandomPhrase());
 // logPhrase(game.getRandomPhrase());
 // logPhrase(game.getRandomPhrase());
 // logPhrase(game.getRandomPhrase());
 // logPhrase(game.getRandomPhrase());
 // logPhrase(game.getRandomPhrase());


// const game = new Game();
// const overlay = document.getElementById('overlay');
// overlay.style.display = 'none';
// const randPhrase = game.getRandomPhrase();
// randArr = randPhrase.phrase.split("");
// const map1 = randArr.map( function (letter) {
//   if (letter === ' '){
//     return `<li class="space">${letter}</li>`
//   } else { return `<li class="hide letter ${letter}">${letter}</li>`;
// }
// });
// let noCommas = map1.join('');
// textSpot = document.getElementById('phrase').getElementsByTagName('ul')[0];
// textSpot.textContent = '';
// textSpot.insertAdjacentHTML("afterbegin", noCommas);

// const game = new Game();
// game.startGame();
// console.log(`Active Phrase - phrase: ${game.activePhrase.phrase}`);
 // const myArray = text.split(" ");
 // let word = myArray[1];
 // console.log(word);
 // // const phrase = new Phrase('Life is like a box of chocolates');
 // console.log(`Phrase - phrase: ${phrase.phrase}`);
